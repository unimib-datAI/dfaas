apiVersion: v1
kind: Service
metadata:
  name: stick-table-exporter
  labels:
    app: stick-table-exporter
spec:
  type: NodePort
  selector:
    app: stick-table-exporter
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
      nodePort: 30808
